{
  "$schema": "https://deno.land/x/denon@2.4.9/schema.json",
  "env": {
    "TOKEN": "abc"
  },
  "scripts": {
    "start": {
      "cmd": "deno run --allow-read --unstable server.ts",
      "allow": [
        "env",
        "net"
      ],
      "env": {
        "PORT": "3000"
      }
    }
  }
}